import {
  DEFAULT_VOLUME,
  PITCH_BAR,
  PITCH_BEAT,
  PITCH_DIVISION,
} from "./metronomeConfig";

const soundState = {
  bar: {
    volume: DEFAULT_VOLUME,
    pitch: PITCH_BAR,
  },
  beat: {
    volume: DEFAULT_VOLUME,
    pitch: PITCH_BEAT,
  },
  division: {
    volume: DEFAULT_VOLUME,
    pitch: PITCH_DIVISION,
  },
};

co;

export { soundState };
