{
  "name": "metronome-ts",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --open --host",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest --ui",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/ui": "^0.25.5",
    "typescript": "^4.6.4",
    "vite": "^3.2.3",
    "vitest": "^0.25.5"
  }
}